# Worktree Manager 🪾

```
Usage: worktree-manager [OPTIONS] <COMMAND>

Commands:
  get-root  
  init      
  add       
  remove    Remove worktree
  list      List available worktrees
  pick      print worktree path
  help      Print this message or the help of the given subcommand(s)

Options:
      --json     
  -h, --help     Print help
  -V, --version  Print version
```
